@layout.main()
  <div class="flex flex-row pl-5">
    <img class="h-full size-52" src="{{ app.headerImage }}" />
    <h1 class="text-white text-3xl pl-4">
      {{ app.name }}
    </h1>
  </div>

  <p class="text-white">
    {{ app.shortDescription }}
  </p>
@end
